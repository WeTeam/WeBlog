@using Sitecore.Modules.WeBlog.Extensions
@using Sitecore.Modules.WeBlog.Globalization
@model Sitecore.Modules.WeBlog.Items.WeBlog.CategoryItem[]

<div class="wb-entry-categories">
    <h3>@Translator.Render("POSTED_IN").ToHtmlString()</h3>
    <ul>
        @foreach (var categoryItem in @Model)
        {
            <li>
                <a href="@categoryItem.GetUrl()">
                    @Html.Sitecore().Field("Title", categoryItem)
                </a>
            </li>
        }
    </ul>
</div>