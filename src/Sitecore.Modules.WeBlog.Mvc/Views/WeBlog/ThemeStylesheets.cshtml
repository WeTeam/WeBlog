@model Sitecore.Modules.WeBlog.Mvc.Model.Theme

@if(Model.ThemeFiles != null)
{

    foreach (var stylesheet in Model.ThemeFiles.Stylesheets)
    {
        <link rel="stylesheet" type="text/css" href="@stylesheet.Url"/>
    }
}
